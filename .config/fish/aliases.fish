# Git
alias gc "git clone"
alias gf "git fetch"
alias gp "git pull --rebase"

# System tools
alias ls "exa"
alias lsl "exa -la"

# Remove or Delete?
alias rr "rmtrash"
alias del "rm"

# Use neovim
alias vi "nvim"

# Emacs
alias e "emacs"

# Babashka
alias bbc "bb -m cmd"
