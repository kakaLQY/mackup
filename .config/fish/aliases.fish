# Git 
alias gc "git clone"
alias gf "git fetch"
alias gp "git pull --rebase"

# System tools
alias ls "exa"
alias lsl "exa -la"

# Remove or Delete?
alias rm "rmtrash"
alias del "rm"

# Use neovim
alias vi "nvim"

# Emacs
alias e "emacs"
